# Changelog

เลขเวอร์ชันอยู่ที่ `src/version.ts` ที่เดียว หัวข้อล่าสุดในไฟล์นี้ต้องตรงกับค่านั้นเสมอ
ทุกครั้งที่ deploy ขึ้น production ต้องขยับเลข + เพิ่มหัวข้อใหม่ที่นี่ (ดู `AGENTS.md` §Versioning)

## 1.2.2 — 2026-09-17

- หน้าหนี้ กยศ.: ตารางทั้งสามเลื่อนในกรอบตัวเองบนจอแคบแล้ว ไม่ดันหน้าทั้งหน้าให้กว้างเกินจออีก
  ปุ่มเลือกแผนขึ้นบรรทัดใหม่ได้ และลดขอบการ์ดบนมือถือให้เหลือความกว้างสำหรับตารางมากขึ้น

## 1.2.1 — 2026-09-17

- หน้าหนี้ กยศ.: ช่อง "ลองปรับตัวเลขดู" รอให้พิมพ์นิ่งก่อนค่อยคำนวณใหม่ (เดิมยิงคำนวณทุกตัวอักษร)
  และระหว่างคำนวณไม่ล้างทั้งหน้าเป็นโครงเปล่าอีกแล้ว ตัวเลขเดิมค้างไว้พร้อมวงกลมหมุนบอกสถานะ

## 1.2.0 — 2026-09-17

- หน้าใหม่ "หนี้ กยศ." คำนวณแผนปลดหนี้จากกติกาจริงของกองทุน: ตาราง Step Up 15 งวด
  (เงินต้นที่ต้องจ่ายเพิ่มขึ้นทุกปี), ดอกเบี้ย 1% ต่อปีเดินรายวัน, ลำดับตัดชำระตาม พ.ร.บ. 2566
- บอกว่าจะปิดหนี้ได้เดือนไหน ต้องจ่ายอีกเท่าไร และเทียบสามทางเลือกให้เห็นกัน:
  เก็บออมแล้วปิดทีเดียว · โปะเข้า กยศ. ทุกเดือน · จ่ายขั้นต่ำอย่างเดียว
- มีแถบตรวจว่าเลขที่กรอกถูกไหม โดยเทียบยอดครบกำหนดปีนี้กับที่แอป กยศ. แจ้ง

## 1.1.0 — 2026-09-08

- แสดงเลขเวอร์ชันมุมล่างขวาทุกหน้า (รวมหน้าเข้าสู่ระบบ) อ่านจาก `GET /api/me`
- คู่มือระบบ: ปุ่ม Guide ไฮไลต์ทีละขั้นในทุกหน้า + หน้า `/help` รวมคู่มือทุกหน้าให้อ่าน/พิมพ์
- Server log: บันทึกเหตุการณ์เข้า/ออก/สมัคร และเติม audit ที่ยังขาดในทุก route ที่แก้ข้อมูล
- หน้า Log สำหรับแอดมิน (ตั้งค่า → บันทึกระบบ) ดูข้ามผู้ใช้ได้ผ่าน `?scope=all`

## 1.0.0 — 2026-09-05

- ระบบตามแผนเดิมครบ Slice 1–8: รับ statement จาก Gmail (SCB/KBank), ledger integrity,
  แดชบอร์ด, วางแผนรายเดือน, รายได้/ผ่อน, คลังเอกสารภาษี, ประมาณการภาษี
